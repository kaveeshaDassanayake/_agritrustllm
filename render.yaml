services:
  - type: web
    name: agritrust-llm
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --chdir backend
    envVars:
      - key: HF_API_TOKEN
        sync: false
      - key: PYTHON_VERSION
        value: 3.11.11
